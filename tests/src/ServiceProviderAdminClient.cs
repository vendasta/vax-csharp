using Vendasta.Vax;

namespace Vendasta.Sso
{
    public class ServiceProviderAdminClient: ServiceProviderAdminGeneratedClient 
    {
        public ServiceProviderAdminClient(Environment env, float defaultTimeout = 10000): base(env, defaultTimeout)
        {
        }
    }
}
